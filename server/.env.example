# Service port (inside container)
PORT=3000

# MongoDB Connection (Docker network, use service name)
MONGO_URI=mongodb://mongoserver:27017/afs_db

# JWT Secret
JWT_SECRET=afs-super-secret-key-2025-change-me-in-production

# Embedding Backend Configuration
# Backend选择: ollama (本地免费) | openai (需要API Key)
EMBEDDING_BACKEND=ollama

# Ollama Configuration
# 注意: Docker环境中，端口可能需要映射到不同端口（如11437）
OLLAMA_BASE_URL=http://localhost:11437
EMBEDDING_MODEL=bge-m3

# OpenAI Configuration (备用，仍需要API Key)
# OPENAI_API_KEY=your-api-key-here

# 注意: BAAI/bge-m3模型文件约2.2GB，需要下载到server/models/embedding_model/
# 首次运行前，请执行: npm run download:embedding-model
# 或者从 https://modelscope.cn/api/v1/models/BAAI/bge-m3/resolve/download 手动下载

# 模型下载完成后，需要在Ollama中加载模型:
# 1. 复制模型到Ollama模型目录 (Docker: /root/.ollama/models, 本地: ~/.ollama/models)
# 2. 创建Modelfile (如果需要)
# 3. 运行: ollama create bge-m3 -f Modelfile
# 4. 验证: ollama list
