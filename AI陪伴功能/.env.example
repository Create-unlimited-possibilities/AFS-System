# Phase 2 环境配置示例

# LLM 配置
# 设置为 true 使用 API LLM，false 使用本地 Ollama
USE_API_LLM=false

# API LLM 配置（可选，当 USE_API_LLM=true 时生效）
API_LLM_PROVIDER=openai        # 可选：openai, anthropic, deepseek
API_LLM_KEY=sk-proj-your-api-key-here
API_LLM_BASE_URL=https://api.openai.com/v1  # API 基础 URL
API_LLM_MODEL=gpt-4o-mini      # API 使用的模型

# 本地 Ollama 配置（当 USE_API_LLM=false 或 API 失败时生效）
OLLAMA_BASE_URL=http://modelserver:11434
OLLAMA_MODEL=qwen2.5
OLLAMA_MODEL_PATH=@model\models\blobs\qwen2.5

# 降级策略（可选）
# api-local: 优先使用 API，失败后使用本地 Ollama
# local-api: 优先使用本地 Ollama，失败后使用 API
# none: 不使用降级，只使用配置的方式
FALLBACK_STRATEGY=api-local

# 其他配置
NODE_ENV=development
JWT_SECRET=your-jwt-secret
MONGO_URI=mongodb://localhost:27017/afs_test

# 文件存储路径
STORAGE_PATH=/app/storage

# 日志配置
LOG_LEVEL=info
LOG_FILE=logs/app.log